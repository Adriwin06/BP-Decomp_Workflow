// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

	}

}

// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

	}

}

// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct SkinData;

		struct DetachedPartRenderEvent;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationOutputInterfaceForEntityModules;

		struct DeformationDebugComponent;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

	}

}

// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationDebugComponent;

	}

}

// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct VehicleLocatorData;

		struct DeformationDebugComponent;

	}

}

// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct VehicleLocatorData;

		struct DeformationDebugComponent;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct AddDeformationModelEvent;

		struct RemoveDeformationModelEvent;

		struct DeactivateDeformationModelEvent;

		struct SetModelCollisionEvent;

		struct SetModelCullingGroupEvent;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct DeformationInputInterface;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct SkinData;

		struct DetachedPartRenderEvent;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationOutputInterfaceForEntityModules;

		struct ImpulsePasser;

		struct CollidableBody;

		struct ImpulseParams;

		struct PenetrationContact;

		struct PenetrationSolver;

		struct DeformationSensor;

		struct StoredImpulseContact;

		struct TagPoint;

		struct IKDrivenPoint;

		struct IKBodyPart;

		struct BurnoutBodyPartID;

		struct PhysicalBodyPart;

		struct PhysicalBodyPartPool;

		struct DetachedPartManager;

		struct BurnoutWheelBodyID;

		struct PhysicalWheel;

		struct DetachedWheelManager;

		struct ContactTime;

		struct ContactOrder;

		struct VehicleRigidBody;

		struct DeformationManager;

		struct DeformationDebugComponent;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct AbsorptionGraph;

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct AddDeformationModelEvent;

		struct RemoveDeformationModelEvent;

		struct DeactivateDeformationModelEvent;

		struct SetModelCollisionEvent;

		struct SetModelCullingGroupEvent;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct DeformationInputInterface;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct SkinData;

		struct DetachedPartRenderEvent;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationOutputInterfaceForEntityModules;

		struct ImpulsePasser;

		struct CollidableBody;

		struct ImpulseParams;

		struct AbsorptionTable;

		struct PenetrationContact;

		struct PenetrationSolver;

		struct StoredContact;

		struct DeformationSensor;

		struct StoredImpulseContact;

		struct TagPoint;

		struct IKDrivenPoint;

		struct IKBodyPart;

		struct BurnoutBodyPartID;

		struct PhysicalBodyPart;

		struct PhysicalBodyPartPool;

		struct DetachedPartManager;

		struct BurnoutWheelBodyID;

		struct PhysicalWheel;

		struct DetachedWheelManager;

		struct ContactTime;

		struct ContactOrder;

		struct VehicleRigidBody;

		struct DeformationAxisAlignedBox;

		struct DeformationManager;

		struct DeformationDebugComponent;

		struct BurnoutJointID;

		struct Joint;

		struct JointPool;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct VehicleLocatorData;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct SkinData;

		struct DetachedPartRenderEvent;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationOutputInterfaceForEntityModules;

		struct DeformationDebugComponent;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct SkinData;

		struct DetachedPartRenderEvent;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationOutputInterfaceForEntityModules;

	}

}

// BrnVehicleConstants.h:26
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct StreamedDeformationSpec;

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct VehicleLocatorData;

		struct DeformationDebugComponent;

	}

}

// Wheel.h:30
namespace BrnPhysics {
	// BrnDeformationResetType.h:28
	namespace Deformation {
		// BrnDeformationResetType.h:30
		enum DeformationResetType {
			E_DEFORMATION_RESET_TYPE_NONE = -1,
			E_DEFORMATION_RESET_TYPE_EVENT = 0,
			E_DEFORMATION_RESET_TYPE_CAR_SELECT = 1,
			E_DEFORMATION_RESET_TYPE_EVENT_COUNT = 2,
		}

		struct TagPointSpec;

		struct IKDrivenPointSpec;

		struct BBoxPointSkinData;

		struct BodyPartBBoxSpec;

		struct DeformationJointSpec;

		struct IKBodyPartSpec;

		struct GlassPaneSpec;

		struct WheelSpec;

		struct LocatorPointSpec;

		struct LocatorPointSpecList;

		struct StreamedDeformationSpec;

		struct AddDeformationModelEvent;

		struct RemoveDeformationModelEvent;

		struct DeactivateDeformationModelEvent;

		struct SetModelCollisionEvent;

		struct SetModelCullingGroupEvent;

		struct BrokenJointNotificationEvent;

		struct DetachedPartNotificationEvent;

		struct JointedPartStateEvent;

		struct DeformationInputInterface;

		struct SensorState;

		struct CarState;

		struct DeformationState;

		struct SkinData;

		struct DetachedPartRenderEvent;

		struct DetachedPartCurrentPositionEvent;

		struct GlassSmashOrCrackEvent;

		struct VehicleLocatorData;

		struct VehicleLocatorOutput;

		struct DeformationOutputInterface;

		struct DeformationOutputInterfaceForEntityModules;

		struct ImpulsePasser;

		struct CollidableBody;

		struct ImpulseParams;

		struct PenetrationContact;

		struct PenetrationSolver;

		struct DeformationSensor;

		struct StoredImpulseContact;

		struct TagPoint;

		struct IKDrivenPoint;

		struct IKBodyPart;

		struct BurnoutBodyPartID;

		struct PhysicalBodyPart;

		struct PhysicalBodyPartPool;

		struct DetachedPartManager;

		struct BurnoutWheelBodyID;

		struct PhysicalWheel;

		struct DetachedWheelManager;

		struct ContactTime;

		struct ContactOrder;

		struct VehicleRigidBody;

		struct DeformationManager;

		struct DeformationDebugComponent;

	}

}

